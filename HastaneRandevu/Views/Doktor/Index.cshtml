@model List<Doktor>

@if (TempData["basarili"] != null){
    <div class="row pt-3 pb-2">
    <h2 style="font-family:'Calibri'" >@TempData["basarili"]</h2>
    </div>
}
else{
    <div class="row pt-3 pb-2">
    <h2 style="font-family:'Calibri'" >Doktorlar</h2>
    </div>
}


<div class="row pt-3 pb-2">
<table class="table table-bordered">
    <thead>
    <tr>
        
        <th>
            Doktor Adi
        </th>
        <th>
            Doktor Soyadi
        </th>
        <th>
            Bolum
        </th>
        
        
    </tr>
    </thead>

    <tbody>
        @foreach(var doktor in Model)
        {
            
        
        <tr>
            
            <td> @doktor.ad </td>
            <td> @doktor.soyad </td>
            <td> </td>
            <td>
                <a asp-controller="Doktor" asp-action="Guncelle" asp-route-id="@doktor.Id" type="button" class="btn btn-outline-success" style="width:125px">Guncelle</a>
            </td>
            <td>
                <a asp-controller="Doktor" asp-action="Sil" asp-route-id="@doktor.Id" type="button" class="btn btn-outline-danger" style="width:125px">Sil</a>
            </td>
        </tr>
        }
    </tbody>
        
</table>
</div>

<div class="row pt-3 pb-2">
    <div>
        <a asp-controller="Doktor" asp-action="Ekle" class="btn btn-lg btn-primary" type="button">
            Doktor Ekle
        </a>
    </div>
</div>